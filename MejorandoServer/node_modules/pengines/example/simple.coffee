pengines = require './../src/pengines'

peng = pengines(
        server: "http://localhost:3020/pengine",
        chunk: 2,
        sourceText: 'animal(cat).\n' + 'animal(dog).\n'
        ask: 'animal(Animal)'
    )
    .on 'success', (result) ->
        for resultData in result.data
            console.log resultData.Animal

